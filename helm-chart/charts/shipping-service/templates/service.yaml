apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: {{ .Values.global.namespace }}
spec:
  clusterIP: None
  selector:
    app: shipping-service
  ports:
  - port: 3011
    targetPort: 3011
