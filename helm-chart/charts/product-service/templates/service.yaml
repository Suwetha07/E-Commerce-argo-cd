apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: {{ .Values.global.namespace }}
spec:
  clusterIP: None
  selector:
    app: product-service
  ports:
  - port: 3003
    targetPort: 3003
