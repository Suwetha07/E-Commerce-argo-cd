apiVersion: gateway.kgateway.dev/v1alpha1
kind: GatewayParameters
metadata:
  name: ecommerce-gateway-params
  namespace: {{ .Values.global.namespace }}
spec:
  kube:
    service:
      type: NodePort
